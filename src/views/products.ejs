<!DOCTYPE html>
<html lang="en">
<head>
</head>

<body>
  <h1> Hiper Supercar</h1>
        <% products.map((product)=>{ %>
        <a href="/products/<%products.id%>"><%=product.title %></a>
        <p><%=product.Description%></p>
        <p>ราคา : <%=product.Price %> ล้านบาท</p>
        <%})%>
  </body>
  </html>